[package]
name = "hello_gpu"
version = "0.2.0"
edition = "2021"
default-run = "hello_gpu"
build = "build.rs"

[dependencies]
cust = { git = "https://github.com/Rust-GPU/Rust-CUDA", branch = "main" }
nvml-wrapper = "0.11"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
